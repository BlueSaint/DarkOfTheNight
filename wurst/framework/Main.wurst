package Main

//	This component is the entry point of the framework

import Client
import Levels

// ---------------

init
	getTimer().start(0,()->begin
		Client.lcl.print(
			"Welcome to |cffffaa00Dark of the Night|r, "
			+ Client.lcl.p.getNameColored(),30)
	end)